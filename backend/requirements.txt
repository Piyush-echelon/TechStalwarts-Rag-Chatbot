# Web framework
fastapi>=0.115.5
uvicorn[standard]>=0.32.1
python-multipart>=0.0.19

# Pydantic / config
pydantic>=2.10.3
pydantic-settings>=2.6.1
python-dotenv>=1.0.1

# LangChain ecosystem (lightweight core only)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-groq>=0.2.0
langchain-community>=0.3.0
langgraph>=0.2.0

# LLM — Groq (fast, free inference)
groq>=0.13.0

# Vector math — pure-numpy in-memory vector store (no native extensions)
numpy>=2.0.0

# PDF processing
pymupdf>=1.24.0

# Hybrid search (BM25)
rank-bm25>=0.2.2

# Utilities
httpx>=0.27.0
